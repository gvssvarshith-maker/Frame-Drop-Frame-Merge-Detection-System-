# Frame-Drop-Frame-Merge-Detection-System-
 a system that detects temporal inconsistencies in a video stream, specifically identifying: Frame Drops – when one or more frames are missing Frame Merges – when multiple frames are blended or incorrectly combined
